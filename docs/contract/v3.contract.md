# Acceptance contract: Jet Fighters v3 (TMS1370 rebuild)

Authored from `docs/prd/jet-fighters-v3.md` before decomposition. This is the
contract the cold exit verifier executes against the assembled v3 product. Its
sha256 is recorded at freeze; a mid-run edit aborts the run rather than
certifying against a moved target.

The class is `interactive`, as v2's was, and for the same reason: no cold agent
can judge whether the tube looks like the tube. What changed is the machine
underneath it, so almost every tier-1 criterion moved even where its intent did
not.

## What this contract is guarding against

Three failures, each with a name and a precedent in this repository.

### A facade that scores points

v1 shipped 100% merged and was, in the owner's verdict, a facade: it rendered a
convincing scope, so every process signal read "done". v2's contract encoded the
fix - **each tier-1 criterion must be falsified by an artifact that merely looks
finished** - and it held. Criteria here lead with a positive requirement that
absence cannot satisfy. V9 is the sharpest case: "the HMCS44 toolchain is gone"
is satisfied by an empty repository, so V9 also requires the TMS1370 core to be
present in the built bundle.

The rebuild adds a second facade shape v2 could not have: **a TMS1370 in name
that behaves like the HMCS44 it replaced**. A sequential program counter, an
unbounded output vocabulary, or a four-deep stack would all assemble, run, and
look finished. V1, V2 and V4 each carry a conjunct that such a build fails.

### Criteria narrower than the hardware

`docs/evidence/open-questions.md` §3b records v2's contract failing correct work
twice:

- **V1** asserted "highest assembled address <= 2047" against a 2048+128-word
  HMCS44 ROM whose upper 128 are the pattern region. A correct ROM defining a
  BCD score table writes there, and a verifier taking the maximum of the address
  column would have failed it.
- **V5** asserted "the burst is shorter than 150 ms". The bound itself is sound -
  `audio-reference.md` records missileFire as `< 150 ms (measured ~20 ms)`. What
  failed was the *method* the criterion implied: first-to-last edge **was** the
  burst against the bootstrap ROM, but against the full game the same window
  holds several sounds, so it read 445 ms where the isolated burst measures
  40.9 ms. A correct ROM failed a correct bound.

Both faults are the same mistake: something true of the artifact in front of the
author - an address ceiling, a measurement method - promoted to a property of the
machine. Two consequences here.
Every numeric bound below is either a hardware constant that cannot move
(2048 words, 128 nibbles, 9 grids, 12 plates, 32 PLA entries) or a **measured
band cited from `docs/evidence/audio-reference.md`** - never a duration or rate
observed once and generalised. And where R6 leaves the instruction rate
provisional, V10 requires provenance and named constants rather than asserting a
value the run is explicitly not yet entitled to.

### The gate defect, which is still live

§3c records that `validate_completion.py` consults tier-1 failures when deciding
a verdict and never tier-3, while `_is_signed()` treats any non-empty
`operator_signoff` as assent. A **failed tier-3 criterion plus a sign-off object
therefore certifies `PASS`** - the exact escape hatch tier-3 exists to close.

This was re-checked against toolkit 1.56.0 while authoring this contract:
`_tier_failures` is called for tier 1 and tier 2 only. The defect is live. It is
a floor artifact in the plugin, not fixable from this repository, so v3 defends
against it in two ways instead:

1. **The run's own rule, binding regardless of the validator's return value:**
   while any tier-3 criterion is recorded `result: fail`, the v3 completion
   record carries **no `operator_signoff` key**, exactly as v2's did. It then
   reads `AWAITING-TIER3-SIGNOFF` rather than falsely certifying.
2. **The tier-3 surface is paired, not sole.** Every perceptual claim in V12 that
   can be observed mechanically has a tier-1 twin (V5 for the display's address
   space and PWM, V8 for the audio bands, V10 for cadence provenance), so a
   signed-off tier-3 failure cannot be the only place a regression hides.

## Blocked artifacts are recorded, never skipped

Four requirements gate on MAME romset artifacts nobody in this project has yet -
`tms1100_common2_micro.pla`, `ginv.svg`, `tms1100_ginv_output.pla` and `mp2110`.
The PRD names this the top-line risk.

**No criterion below is conditional on an artifact arriving.** A criterion that
cannot be driven because an artifact is absent is recorded `result: undriven`
(or listed in `couldnt_drive`), which forces `PARTIAL` and never `PASS`. That is
the honest outcome: v3 with an invented segment addressing is not v3, and a
contract that let "the romset did not arrive" satisfy a criterion would be
satisfiable by absence - the precise fault the first section names.

## Drive surface

Tier-1 criteria are driven in Node, not a browser, because `src/machine/` never
touches the DOM. v2's probe harness carries forward and its JSON shape is
unchanged, but three of its values mean something different on this machine and
v3 must update it accordingly:

`tools/probe/machine-probe.ts`, runnable as
`npx vite-node tools/probe/machine-probe.ts [--cycles N] [--input SPEC] [--emit-edges]`,
writing a single JSON object to stdout:

```
{
  "romWords":     int,           // assembled program words, 8-bit, program region only
  "ramNibbles":   int,           // RAM high-water mark, <= 128
  "strobeCycles": int,           // machine cycles executed (one per six oscillator pulses)
  "gridsStrobed": [int],         // distinct R-line grids driven, expect 0..8 - there is no grid 9
  "snapshots": [
    { "atCycle": int, "litSegments": [[grid, plate, duty], ...] }
  ],
  "speakerEdges": [[cycle, level], ...]   // R15 transitions, when --emit-edges; there is no D port
}
```

`snapshots[0]` is a baseline taken once the machine has completed at least one
full display sweep and before any input is injected; it is always present. Each
`--input` event appends one further snapshot taken after that event has been
strobed in.

`--input SPEC` takes `name=value@cycle` (e.g. `lever=up@120000`, `fire@120000`)
and injects the control through the K matrix exactly as the case wiring does -
K1/K2/K4 returned from a strobe on R9 or R10, K8 unstrobed for fire - not by
poking game state.

**Cycle counts in the actions below mean something different from v2's.** One
instruction per six oscillator pulses at ~350 kHz is roughly 58,000 machine
cycles per second against v2's 400,000, so v2's `--cycles 400000` bought about
one second and here buys about seven. Where a criterion needs only "at least one
full sweep", a generous round figure is given and any refinement of the
oscillator constant leaves it generous. Where a criterion waits on a *timed game
event* - V8's battleship, ~19.8 s between arrivals - the figure is stated as the
event interval it must cover, to be recomputed from R6's constant rather than
copied, because a rate refinement would otherwise silently truncate the window
and fail a correct build.

## Floors, and why they sit where they do

- **>= 200 assembled program words** of 2048. Far below a real game program, far
  above any stub.
- **>= 600 passing tests.** v2 stands at ~1288 static `it`/`test` sites; v3
  deletes 226 in `tools/hmasm/` and rewrites ~370 under `src/machine/cpu/`, so
  692 survive before a single new test is written. The floor clears on the
  survivors alone and is unreachable by a gutted repository.

```yaml
class: interactive
criteria:
  - id: V1
    tier: 1
    action: "Run: npm ci && npx vite-node tools/tmsasm/cli.ts asm/jetfighter.asm --listing /tmp/jf.lst ; capture the exit code and read the listing."
    observation: "Exit code is 0. The listing reports program-region word count and RAM high-water mark as named fields (not inferred from the maximum of an address column); words <= 2048 and >= 200; RAM <= 128 nibbles; every emitted word is an 8-bit value 0-255. Additionally, at least one page shows source order differing from emitted physical order, which is the LFSR placement rule doing its work - an assembler that lays instructions down sequentially fails this conjunct while producing an otherwise valid-looking listing. A missing assembler, a missing asm/jetfighter.asm, or an overflow past 2048 fails."
  - id: V2
    tier: 1
    action: "Run: npm ci && npm test -- --run --reporter=json > /tmp/jf-tests.json ; parse the JSON summary and the test names."
    observation: "numFailedTests is 0 and numPassedTests >= 600. The passing set includes, under src/machine/cpu/: per-opcode semantics tests naming TMS1000-family mnemonics; a test asserting the 64-state LFSR program-counter sequence and that it is a bijection over its states; a test that a taken branch inside a subroutine does not change the page while one outside it does; a test that CALL inside a subroutine saves no return address; and an encode(decode(op)) == op round-trip across the opcode space. No test file under tools/hmasm/ contributes to the count. Zero tests, absent core tests, or any failure fails the criterion."
  - id: V3
    tier: 1
    action: "Read the tools/tmsasm/ test files and the V2 JSON; identify the tests asserting rejection of each of the PRD R2 silent-failure classes."
    observation: "Five distinct passing tests are present, one per class: LFSR placement; a page-crossing branch inside a subroutine; a CALL reachable from inside a subroutine; SETR/RSTR with X >= 4; and an instruction between a status-setting test and its branch. Each drives a violating program through the assembler and asserts it fails to assemble. The CALL-inside-subroutine rejection message names the offending call site, not only the callee. Fewer than five, or a test asserting only that valid programs assemble, fails - presence of the rejections is the requirement and absence cannot satisfy it."
  - id: V4
    tier: 1
    action: "Assemble as in V1 and read the listing's O output PLA section; then run the rom-atlas-conformance suite from the V2 run."
    observation: "The listing carries the PLA as its own section with exactly 32 entries, declared in the assembly source and assembled into the machine image. Entry 0 is all plates dark (pla[0] === 0), so reset writes darkness rather than garbage. The conformance suite asserts closure: every (grid, low-8-plate mask) the ROM drives across the scenario space appears in the 32-entry table. A mutation case is present that removes or alters one entry and expects conformance to fail, proving the closure assertion is armed rather than vacuous. A design with more than 32 entries, or one whose closure check passes with an emptied table, fails."
  - id: V5
    tier: 1
    action: "Run: npx vite-node tools/probe/machine-probe.ts --cycles 250000 ; parse the JSON on stdout, and read src/machine/tube/atlas.json."
    observation: "strobeCycles > 0. gridsStrobed contains every value 0 through 8 and no value 9 or above - nine grids on R0-R8, and the absence of a tenth is asserted, not merely unmentioned. Every plate index in atlas.json lies in 0-11. snapshots[0].litSegments is non-empty with at least one segment whose duty is strictly between 0 and 1, proving PWM accumulation rather than a static write. score_label is not on grid 9. The machine is executing our ROM and driving the tube through the TMS1370's actual address space."
  - id: V6
    tier: 1
    action: "Read docs/evidence/ginv-addressing.md and src/machine/tube/atlas.json, and run the atlas test suite from the V2 run."
    observation: "docs/evidence/ginv-addressing.md exists and records, per segment id, the (grid, plate) pair read from the romset's ginv.svg together with its provenance; it covers all 94 segments. A passing test asserts atlas.json agrees with that record for every segment. The 94 traced outlines and atlas.test.ts's measured frame assertion are unchanged - the frame assertion uses getSegmentById and bounds only, so re-addressing cannot reach it. A hand-invented assignment fails on the provenance document, which is the point: R7's comparison is meaningless against addresses that are not Gakken's."
  - id: V7
    tier: 1
    action: "Run: npx vite-node tools/probe/machine-probe.ts --cycles 250000 --input lever=up@120000 ; then again with --input fire@120000 ; compare snapshots. Read the board test suite for the strobe and K8 assertions."
    observation: "In the lever run both snapshots are present and their litSegments differ in the launcher lane region. A passing test asserts that at most one of R9/R10 is high on any cycle across a full sweep, so the input columns cannot superimpose. A passing test drives the fire contact while both strobe columns are low and observes the program respond, proving K8 is read unstrobed - a build routing fire through R9/R10 fails it, and that is a real behavioural difference from v2 rather than a detail. Identical snapshots, a missing second snapshot, or a strobed fire path fails."
  - id: V8
    tier: 1
    action: "Run the probe with --emit-edges, --input fire at roughly two seconds in, and --cycles covering at least two battleship arrival intervals as audio-reference.md records them (>= 1800000 at the instruction-rate constant V10 requires; recompute from that constant rather than copying the figure). Reconstruct the R15 waveform, window each sound event separately, and take the FFT of each window. Read docs/evidence/audio-reference.md."
    observation: "speakerEdges is non-empty, every edge originates from a SETR/RSTR write to R15, and no D-port write path exists in the build. Each sound is measured over its own isolated burst, split on the silence gap, and never as first-to-last edge across the capture - that method, not the 150 ms bound it produced, is what open-questions section 3b records as v2's fault. The fire blip's dominant frequency falls within 1480-1632 Hz and its duration within the figure audio-reference.md's acceptance summary gives for missileFire. The battleship arrival sounds continuously for its recorded ~4.0 s with a pulse repetition rate inside the measured 79-111 Hz band, and is distinguished from the loss sound by decay floor as well as band, because an ~85-93 Hz buzz sits inside that band on any silicon. Every band and duration is read from audio-reference.md at verification time, not from this contract."
  - id: V9
    tier: 1
    action: "Run: npm run build ; then search the emitted dist/ bundle for machine-layer identifiers, run rg -i hmcs4 across the tree, read package.json, and list tools/."
    observation: "All five hold: build exits 0; the dist/ bundle contains the TMS1370 core and tube-renderer code originating from src/machine/; package.json declares zero runtime dependencies; rg -i hmcs4 matches only under docs/ (the PRDs, the research notes and open-questions.md section 7, which are history and must keep it); and tools/tmsasm/ exists while tools/hmasm/ and asm/example.asm do not. An empty or unbuilt repository fails on the first three conjuncts, which is why they lead."
  - id: V10
    tier: 1
    action: "Read the exported timing constants in src/machine/, the provenance document recording how the instruction rate was obtained, and the timing assertions in tools/probe/."
    observation: "The oscillator frequency and the divide-by-six are two separately named exported constants, not one fused cycles-per-second number, so refining one does not silently move the other. A committed document records how the instruction rate was obtained - either the non-circular route (mp2110 obtained, R0's verified opcode set applied, the sweep loop statically counted, the measured refresh divided by it) or an explicit statement that it remains provisional pending that artifact. Provisional is an acceptable answer here; silent is not. No timing assertion in tools/probe/ compares against a raw cycle literal sized for a different clock: each derives from a named constant or a measured band in docs/evidence/audio-reference.md. A surviving 400 kHz-era literal such as BURST_GAP_CYCLES = 8000 fails."
  - id: V11
    tier: 1
    action: "Run the R7 comparison harness against the repository's own ROM with no romset present, and read its output."
    observation: "The harness runs and reports the comparison surface - lit segment set over time, speaker edges, score - against our own machine image, exiting non-zero only on a genuine mismatch and not on the absence of a romset. It accepts an alternative machine image plus a supplied output PLA as its comparison target, so the original dump can be interpreted when it arrives. No ROM content from the original is present in the repository. A harness that cannot run without the romset fails, because that makes it unusable for the work it exists to support."
  - id: V12
    tier: 3
    action: "Operator launches the built site, plays it at skill 1 and skill 3 beside the physical CGL unit and the reference gameplay video in docs/evidence/, and pastes the observed output into the completion record."
    observation: "Stepping rhythm, PWM flicker and brightness shimmer, the cyan and red phosphor regions under the filter overlay, segment shapes, the segment-display score, the sound blanking that makes every beep a visible blink, and sound timing are recognisably the 1979 unit rather than an imitation of it - and specifically, the tighter machine shows: nine grids rather than ten, the coarser output vocabulary, and the cadences a 58 kHz instruction rate produces. Human-mandatory: a cold agent structurally cannot observe this. While this criterion is recorded result fail, the completion record carries no operator_signoff key, because the validator consults tier-1 failures only and a sign-off alongside a tier-3 failure would falsely certify PASS."
```
